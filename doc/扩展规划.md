# 扩展方向规划

> **定位**：mentor 以学习为主、不赶成果。所以下面的优先级按**概念学习价值**排，
> 而不是按"最快出结果"或"做新成果"排。6/10 是学习节点，不是硬产出压力。

## 当前进度：已完成的闭环

一条完整的"挖 → 评 → 测"链路 + 一批 GP 进阶模块：

- **挖**：GP 引擎（表达式树 / 三类算子 / 锦标赛选择 / 子树交叉变异）
- **评**：IC / ICIR（Rank IC，截面预测力）
- **测**：成本回测（不重叠调仓 + 换手手续费 + 夏普 / 最大回撤 / 年化）
- **进阶模块**：ramped half-and-half 初始化、深度门禁、parsimony 简约压力、
  NSGA-II 多目标（含基因型去重保多样性）

**已知但暂未处理**：结果均为样本内，高复杂度因子 ICIR 虚高（过拟合）。

## 待扩展方向（按学习价值分组）

### A. 因子质量与验证 ← 下一站

- **样本外验证 / walk-forward**（首选）：切训练-测试 / 滚动窗口，证明因子非过拟合。
- 因子去冗余 / 正交化：与已有因子库正交，只留增量信息。
- 多重检验校正（deflated Sharpe）：校正"搜上万个挑最好"的乐观偏差。
- 稳健性测试：换股票池 / 时段 / 参数看稳定性。

### B. GP 算法纵深

- **强类型 GP（STGP）**：给算子标类型（截面维 / 时序维 / 标量），只生成合法因子，
  禁止 `rank(rank(x))`、`ts_mean(x, close)` 这类无意义组合。
- 变异算子家族：point / hoist / shrink mutation，不同探索粒度。
- lexicase 选择 / 岛模型：更强的多样性保持，缓解早熟。
- 热启动：用已知 alpha（Alpha101 等）做初始种群。

### B+. 适应度函数纵深（搜索目标层）← 当前重点

> A–D 跑完的最大教训：杀死每个因子的头号元凶是**过拟合 / regime 依赖**。
> 而现有适应度奖励的恰是「样本内峰值 ICIR」——正是过拟合的东西。
> 最对症的优化不在算子 / 选择算子，而在**搜索目标本身**。

- **① 时序稳定性适应度**（最优先）：训练段切 K 个子区间各算 ICIR，适应度取稳健聚合
  （`|mean| − λ·std`，符号翻转会被 std 惩罚、被 mean 抵消）。把"跨期都稳"**前置进搜索目标**，
  逼搜索偏向能扛样本外的因子。只改 `make_objective` 一处，挂进现有 NSGA。
- **② 正交性压力**：适应度惩罚与 `amount` 的相关（`|ICIR| − λ·|corr(因子, amount)|`），
  逼搜索跳出"幸存者全是流动性"的吸引盆，去找正交新信号。与扩算子叠加。
- **③ hoist / shrink 变异**：直接砍 bloat（`ts_std(ts_std(...))` 平滑堆叠），让前沿更简洁。
  即 B 节"变异算子家族"的对症落地。

### C. 算子集与数据

- 扩充算子：`ts_corr / ts_max / ts_argmax / scale / decay_linear / sign / power` …
- 中性化算子：行业 / 市值中性，剥离风格暴露。
- 引入新数据：基本面、资金流、另类数据。

### D. 回测与组合现实性

- 更真实的回测：涨跌停、停牌、T+1、冲击成本 / 滑点。
- 组合构建：从单因子打分升级到带风险模型 / 约束的组合优化。
- 多因子合成：把 GP 挖的多个因子合成复合因子。

### E. 工程与规模

- 并行评估：适应度评估天然可并行（multiprocessing / joblib），提速数倍。
- 真正用上 H100：因子求值搬 GPU（cupy / cudf），或走下面的 RL 方向。
- 子树求值缓存：相同子树不重复算。
- 实验追踪：配置版本化、随机种子、结果可复现。

### F. 研究前沿（想"超越复现"再看）

- AlphaGen 类（RL 搜 alpha）：用 PPO 代替 GP 搜表达式，吃满 GPU。
- 多目标深化：IC + 换手 + 容量 + 相关性 多目标（已有 NSGA-II 底子）。
- 神经符号 / LLM 辅助因子生成：最前沿、风险高。

## 优先级（学习导向）

| 学习目标                         | 推荐方向                  |
| -------------------------------- | ------------------------- |
| 理解"什么叫真有效"、收口认知闭环 | **A. 样本外验证（先做）** |
| 加深 GP 搜索空间设计的理解       | B. 强类型 GP              |
| 理解并行 / 工程、唤醒 H100       | E. 并行评估               |
| 升维成研究课题                   | F. AlphaGen(RL)           |

## 进度

- **A 因子质量与验证** ✅ walk-forward + 重叠校正(NW) + deflated Sharpe（[验证模块](./验证模块.md)）
- **B GP 算法纵深** ✅ 强类型 GP（[强类型GP](./强类型GP.md)，结论：搜索合法性不是瓶颈）
- **C 算子集与数据** ✅ 扩算子 C1/C2 + reward hacking 修复（[算子与数据](./算子与数据.md)）；C3 正交数据设计完成、执行待数据
- **D 回测现实性** ✅ 涨跌停过滤 + long-only + T+1 开盘成交（[回测现实性](./回测现实性.md)，结论：ICIR 最强 ≠ 实盘最好）

## 适应度函数纵深（B+）进度

笔记：[适应度函数纵深.md](./适应度函数纵深.md)

- **① 时序稳定性** ✅：fold2 NW_t 1.3→2.84，首次在小 N 下通过 deflated Sharpe；暴露回测方向 bug（负 ICIR 因子需翻转再做多头）
- **② 正交性压力** ✅：惩罚口径打偏（只挡 amount 水平、不挡关系类用法），但副产物选出统计质量最高因子 `ts_corr(amount,high)`（NW_t 3.40）。收口结论：纯量价 GP 能挖统计显著信号，但多头落地仍难。
- **③ hoist/shrink 变异** ⬜ 留作后续工程优化（bloat 治理，与主线无关）
- **工程** ✅：joblib 并行评估（8h→1h）+ 统一评估入口 [`evaluate_run.py`](./../scripts/evaluate_run.py)
