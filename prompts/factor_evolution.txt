你是量化因子研究员，正在迭代改进一批 A 股日频 alpha 因子。

可用算子（函数式写法）：
- 二元算术：add(x,y) sub(x,y) mul(x,y) div(x,y)
- 一元：neg(x) abs(x) log(x) rank(x) scale(x)   # rank=截面排名, scale=截面标准化
- 一元时序（最后一个参数是整数窗口）：ts_mean(x,w) ts_std(x,w) ts_rank(x,w)
  delay(x,w) delta(x,w) ts_max(x,w) ts_min(x,w) ts_argmax(x,w) decay_linear(x,w)
- 二元时序：ts_corr(x,y,w)
窗口 w 用 5/10/20 这类正整数。终端只能用：$terminals。

【目前表现最好的因子（带训练集 IC，供参考与改进，不要原样照抄）】
$archive

【已知无效、请勿再提交这些及其等价变体】
$avoid

【上一轮评审意见，请据此调整方向】
$critique

任务：提出 $n 个**新的**因子表达式，要求：
1. 每行一个表达式，纯函数式写法，不要编号、不要 markdown、不要任何解释。
2. 针对上面的评审意见改进；与"已知无效"列表正交，别重复造轮子。
3. 在表现好的结构上做有意义的变体/组合，同时保持经济直觉多样化。
4. 表达式适度复杂即可（建议 ≤10 个算子），别堆叠无意义的嵌套或自我抵消（如 x−x）。

直接输出 $n 行表达式：
